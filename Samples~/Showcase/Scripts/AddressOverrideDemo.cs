namespace Hapbeat.Samples.Showcase
{
    /// <summary>
    /// Z4_Stream demo of the global Player / Group address override.
    /// The actual panel implementation lives in the SDK runtime as
    /// <see cref="HapbeatAddressOverridePanel"/> (screen-space by default);
    /// this class exists only so the Showcase scene keeps its own
    /// sample-scoped component name/namespace.
    /// </summary>
    public class AddressOverrideDemo : HapbeatAddressOverridePanel
    {
    }
}
